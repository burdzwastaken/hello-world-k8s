kind: Service
apiVersion: v1
metadata:
  name: hello-world-k8s
  namespace: default
spec:
  type: LoadBalancer
  selector:
    source: demo-v2
    app: spinnaker
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
